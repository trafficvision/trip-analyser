const React = require('react');

// components
var Nav = require('Nav');

var User = React.createClass({
  render: function () {
    return (
      <div>
        <h1>User Page</h1>
      </div>
    );
  }
});

module.exports = User;
